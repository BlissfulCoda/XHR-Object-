# XHR-Object-
- working with AJAX & XHR object and fetching data from a .txt file using the HTTP Status codes and displaying that data back to the browser.
- used the skeleton.css framework for the button
